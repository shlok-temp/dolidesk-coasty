# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Run artefacts. Regenerated by `rehearse` and `run`, and `out/frames/` holds
# screenshots of whatever desktop the agent was driving -- which on a real run
# is the operator's own screen, so it must never be committed.
out/
tmp/

# Local tooling and credentials
.claude/
.env
.env.*
*.local

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
